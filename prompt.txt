Read ADMIN_UI_REDESIGN_RULES.md before making any further changes to the admin panel.

PROMPT 8 — Order Management

Now restore and redesign ADMIN ORDER MANAGEMENT.

Preserve all existing order functionality.

Do not change the order business logic.

Required functionality:

- list orders
- search orders
- filter orders
- status filter
- date filter if already supported
- vendor filter
- customer filter if supported
- pagination
- order details
- update order status
- cancel order if supported
- view customer
- view vendor
- payment information if already available

PAGE:

Order Management

Subtitle:

Track and manage marketplace orders.

FILTER BAR:

Search order ID/customer

Status

Vendor

Date

TABLE:

Order ID
Customer
Vendor
Items
Amount
Payment
Status
Date
Actions

Status badges:

Pending
Confirmed
Processing
Out for Delivery
Delivered
Cancelled

Use the existing status values if they differ.

ORDER DETAILS:

Create a polished detail drawer/page using existing order information.

Show:

Customer
Vendor
Products
Quantities
Subtotal
Delivery
Total
Payment
Order status
Timeline if existing data supports it

Do not remove any existing order fields.





PROMPT 9 — Customer/User Management

Now restore and redesign ADMIN CUSTOMER / USER MANAGEMENT.

Preserve existing user management functionality.

PAGE:

Customers

Subtitle:

Manage customers using the AapnuBazaar marketplace.

TABLE:

Customer
Email
Phone
Orders
Total Spent
Status
Joined
Actions

Actions supported by the existing application:

View
Edit
Activate/Deactivate
Delete
View Orders

Add:

Search
Status filter
Pagination

If the application has multiple user roles, do not incorrectly merge them.

Clearly distinguish:

Customer
Vendor
Admin

Reuse the existing authorization and role system.

Do not change authentication logic.




PROMPT 10 — Category + Store Management

Now restore and redesign CATEGORY and STORE MANAGEMENT.

Do not remove existing functionality.

CATEGORY MANAGEMENT:

List categories

Create category

Edit category

Delete category

Activate/deactivate if supported

Category fields should use the existing data model.

UI:

Category name
Icon/image
Products
Status
Actions

STORE MANAGEMENT:

List stores

Create/edit store if existing functionality supports it

Vendor
Store name
Category
Location
Status
Products
Orders
Actions

Reuse existing APIs and forms.

Do not invent new database fields.



PROMPT 11 — Admin Users + Settings

Now restore the ADMIN MANAGEMENT and SETTINGS sections.

ADMIN MANAGEMENT:

Show existing administrators.

Fields based on existing data:

Name
Email
Role
Status
Created
Actions

Preserve existing role/permission functionality.

SETTINGS:

Do not create fake settings.

Inspect the existing settings implementation and expose all existing settings through the redesigned UI.

Possible sections if they already exist:

Profile
Marketplace
Notifications
Payment
Delivery
Security
Application settings

Do not remove settings just because they were not visible after the UI redesign.

Preserve all existing save/update functionality.



PROMPT 12 — Search, Filters, Pagination & Modals

Now perform a functionality restoration pass specifically for interactive management features.

Check EVERY admin management page for:

SEARCH
FILTER
SORT
PAGINATION
CREATE
EDIT
DELETE
VIEW
STATUS CHANGE
MODALS
DRAWERS
CONFIRMATION DIALOGS
TOASTS
LOADING STATES
ERROR STATES
EMPTY STATES

For every existing feature:

Verify that the UI control exists.

Verify that it is connected to the existing handler/API.

Verify that the API request is correctly triggered.

Verify that the response updates the UI.

Verify that errors are displayed.

Verify that loading states are displayed.

Verify that success feedback is displayed.

Do not replace working implementations with mock functionality.

If a feature exists in code but is not visible, expose it in the appropriate location.

If a feature is visible but does nothing, reconnect it to the existing implementation.

Do not create duplicate handlers.



PROMPT 13 — Responsive Admin

Now make the entire AapnuBazaar Admin Panel responsive.

Desktop:

1280px
1440px
1920px

Tablet:

768px
1024px

Mobile:

320px
375px
390px
414px

Desktop:

Fixed/collapsible sidebar
Full content area

Tablet:

Collapsible sidebar

Mobile:

Sidebar becomes drawer.

Tables must not break the viewport.

For large management tables use either:

- horizontal scrolling
OR
- responsive card layout

Do not hide important management actions simply because the screen is small.

Forms:

Desktop:
2-column where appropriate

Mobile:
1-column

Cards:

Responsive grid

Charts:

Responsive width

Buttons:

Minimum comfortable touch target

No horizontal page overflow.

Test every admin route at mobile width.


PROMPT 14 — Protect Existing Functionality

Perform a complete regression audit.

The visual redesign must NOT have broken existing AapnuBazaar functionality.

Trace these user journeys:

ADMIN LOGIN
→ DASHBOARD

DASHBOARD
→ VENDORS

VENDORS
→ VIEW VENDOR

VENDORS
→ EDIT VENDOR

VENDORS
→ DELETE/STATUS CHANGE

DASHBOARD
→ PRODUCTS

PRODUCTS
→ CREATE PRODUCT

PRODUCTS
→ EDIT PRODUCT

PRODUCTS
→ DELETE PRODUCT

DASHBOARD
→ ORDERS

ORDERS
→ ORDER DETAILS

ORDERS
→ UPDATE STATUS

DASHBOARD
→ CUSTOMERS

CUSTOMERS
→ CUSTOMER DETAILS

CUSTOMERS
→ CUSTOMER ORDERS

CATEGORIES
→ CREATE
→ EDIT
→ DELETE

SETTINGS
→ SAVE SETTINGS

ADMIN MANAGEMENT
→ CREATE/EDIT/DELETE where supported

For each flow verify:

1. Route works
2. Page loads
3. API request works
4. Data appears
5. Buttons work
6. Forms work
7. Modals work
8. Errors are handled
9. Loading states work
10. Success states work
11. Data refreshes correctly

Do not stop at checking whether buttons are visible.

Actually trace the functionality through the existing code.

If something is broken, identify whether the cause is:

- route
- component
- prop
- state
- handler
- API call
- API response mapping
- conditional rendering
- CSS visibility
- z-index
- overflow
- modal
- authorization

Fix the smallest necessary layer.

Do NOT rewrite working backend logic.


PROMPT 15 — Final UI polish based on your screenshot

Now perform the final visual polish of the AapnuBazaar Admin Panel.

Use the provided dashboard screenshot only as visual inspiration.

DO NOT copy its brand name, logo, text, data or exact content.

AapnuBazaar must remain the brand.

Target visual characteristics:

- Light blue/gray admin background
- White rounded sidebar
- White rounded cards
- Clean dashboard hierarchy
- Blue interaction accents
- AapnuBazaar green branding
- Orange marketplace accents
- Soft shadows
- Rounded 14–20px cards
- Professional typography
- Spacious but efficient layout
- Clear data visualization
- Premium SaaS dashboard appearance

Improve:

- spacing
- alignment
- card hierarchy
- icon consistency
- typography
- table readability
- status badges
- buttons
- filters
- search fields
- empty states
- loading skeletons
- responsive behavior

IMPORTANT:

Do not sacrifice functionality for visual design.

If there is a conflict between visual design and existing functionality, preserve the functionality and adapt the visual design around it.

The final panel should look like:

AAPNUBAZAAR
ADMIN PORTAL

not GoodWell.

Before finishing, run the application and verify all admin routes and major CRUD operations.



